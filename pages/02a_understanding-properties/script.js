import gsap from 'gsap';

gsap.to(".box", {
    opacity: 1,
    rotation: 360,
    borderRadius: "50%",
    background: "#1DD1A1",
    duration: 2,
    delay: 0.5,
    scale: 1.25,
    ease: "sine"

})
